@model IEnumerable<IsTakipSistemi.Models.Birimler>

@{
    ViewBag.Title = "Birimler";
}

<h2>Birimler Listesi</h2>
<p>
    @Html.ActionLink("Yeni Birim Ekle", "Create", new { }, new { @class = "btn btn-primary" })
</p>

<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead class="table-dark">
            <tr>
                <th>Birim Adı</th>
                <th>Personel Sayısı</th>
                <th>İşlemler</th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null && Model.Count() > 0)
            {
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => item.BirimAd)</td>
                        <td>
                            <span class="badge bg-info">@item.Personeller.Count</span>
                        </td>
                        <td>
                            @Html.ActionLink("Detay", "Details", new { id = item.BirimId }, new { @class = "btn btn-sm btn-info" })
                            @Html.ActionLink("Düzenle", "Edit", new { id = item.BirimId }, new { @class = "btn btn-sm btn-warning" })
                            @Html.ActionLink("Sil", "Delete", new { id = item.BirimId }, new { @class = "btn btn-sm btn-danger" })
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="3" class="text-center">Henüz hiç birim eklenmemiştir.</td>
                </tr>
            }
        </tbody>
    </table>
</div>
