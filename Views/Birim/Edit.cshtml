@model IsTakipSistemi.Models.Birimler

@{
    ViewBag.Title = "Birim Düzenle";
}

<h2>Birim Düzenle</h2>

@using (Html.BeginForm("Edit", "Birim", FormMethod.Post, new { @class = "form-group" }))
{
    @Html.AntiForgeryToken()
    <div class="row">
        <div class="col-md-6">
            @Html.HiddenFor(model => model.BirimId)

            <div class="mb-3">
                @Html.LabelFor(model => model.BirimAd, new { @class = "form-label" })
                @Html.TextBoxFor(model => model.BirimAd, new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.BirimAd, "", new { @class = "text-danger" })
            </div>

            <div class="mb-3">
                <button type="submit" class="btn btn-warning">Güncelle</button>
                <a href="@Url.Action("Index")" class="btn btn-secondary">İptal</a>
            </div>
        </div>
    </div>
}
